apiVersion: v1
kind: Service
metadata:
  name: {{ include "nebulacb.fullname" . }}
  labels:
    {{- include "nebulacb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    {{- if .Values.config.metrics.enabled }}
    - port: {{ .Values.config.metrics.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
    {{- end }}
  selector:
    {{- include "nebulacb.selectorLabels" . | nindent 4 }}
