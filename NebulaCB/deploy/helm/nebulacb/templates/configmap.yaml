apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nebulacb.fullname" . }}-config
  labels:
    {{- include "nebulacb.labels" . | nindent 4 }}
data:
  config.json: |
    {{- .Values.config | toJson | nindent 4 }}
